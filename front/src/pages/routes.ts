enum Routes {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  UNMASK = '/unmask',
  SEARCH = '/search',
  VOTE = '/vote',
}

export enum RoutesNames {
  VOTE = 'vote',
  ROOT = 'ROOT',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  UNMASK = 'UNMASK!',
  SEARCH = 'SEARCH',
}


export default Routes;
