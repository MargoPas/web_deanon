enum Routes {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  COUNTER = '/counter',
  UNMASK = '/unmask',
  NEWS = '/news',
  SEARCH = '/search'
}

export enum RoutesNames {
  ROOT = 'ROOT',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  UNMASK = 'UNMASK!',
  NEWS = 'NEWS',
  SEARCH = 'SEARCH',
}


export default Routes;
