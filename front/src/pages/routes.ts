enum Routes {
  ROOT = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  COUNTER = '/counter',
  UNMASK = '/unmask',
  SEARCH = '/search'
}

export enum RoutesNames {
  ROOT = 'ROOT',
  LOGIN = 'LOGIN',
  REGISTER = 'REGISTER',
  UNMASK = 'UNMASK!',
  SEARCH = 'SEARCH',
}


export default Routes;
