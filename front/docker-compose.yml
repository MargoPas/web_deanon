version: '2.4'

services:
  front-regina:
    container_name: front-regina
    image: nginx:1.19.9-alpine
    volumes:
      - ./dist:/var/web-deanon/dist
      - ./docker/nginx:/etc/nginx/conf.d
    restart: always

networks:
  default:
    external:
      name: sample-front